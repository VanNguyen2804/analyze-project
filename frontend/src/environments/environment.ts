export const environment = {
  production: false,
  // Khi chạy local hoặc cùng domain, để trống để gọi tương đối: /api/...
  // Khi deploy Frontend và Backend riêng biệt trên Render, điền URL backend Render vào đây (VD: 'https://analyze-backend.onrender.com')
  apiUrl: ''
};
